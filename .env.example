# External Nova API base (no trailing slash).
VITE_API_URL=https://api.novacasino.games

# Websocket is currently disabled by default because the backend socket is unstable.
# Set to true later when the backend websocket is fixed.
VITE_WS_ENABLED=false

VITE_TELEGRAM_HELP_USERNAME=@novasu
